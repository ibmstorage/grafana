import React from 'react';
import { InlineField, InlineSwitch } from '@grafana/ui';
import { config } from '@grafana/runtime';
import '@grafana/data';

const SecureSocksProxyToggle = ({ labelWidth = 10, ...props }) => {
  const { dataSourceConfig, onChange } = props;
  const handleSwitchChange = (e) => {
    onChange({
      ...dataSourceConfig,
      jsonData: {
        ...dataSourceConfig.jsonData,
        enableSecureSocksProxy: e.target.checked
      }
    });
  };
  return config.secureSocksDSProxyEnabled && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "gf-form" }, /* @__PURE__ */ React.createElement("div", { className: "gf-form gf-form-inline" }, /* @__PURE__ */ React.createElement(
    InlineField,
    {
      htmlFor: "secureSocksProxyEnabled",
      label: "Secure Socks Proxy Enabled",
      labelWidth,
      tooltip: /* @__PURE__ */ React.createElement(React.Fragment, null, "Proxy the datasource connection through the secure socks proxy to a different network. To learn more about configuring the datasource connection proxy,", " ", /* @__PURE__ */ React.createElement(
        "a",
        {
          href: "https://grafana.com/docs/grafana/latest/setup-grafana/configure-grafana/proxy/",
          target: "_blank",
          rel: "noopener noreferrer"
        },
        "click here."
      ))
    },
    /* @__PURE__ */ React.createElement(
      InlineSwitch,
      {
        id: "secureSocksProxyEnabled",
        value: dataSourceConfig.jsonData.enableSecureSocksProxy,
        onChange: handleSwitchChange
      }
    )
  ))));
};

export { SecureSocksProxyToggle };
//# sourceMappingURL=SecureSocksProxyToggle.js.map
