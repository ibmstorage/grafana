import React from 'react';
import { css } from '@emotion/css';
import { useToggle } from 'react-use';
import { getValueFormat } from '@grafana/data';
import { config } from '@grafana/runtime';
import { useStyles2, Collapse, Tooltip, Icon } from '@grafana/ui';
import '../../QueryEditor/types.js';
import 'lodash';
import '../../QueryEditor/EditorList.js';
import { EditorStack } from '../../QueryEditor/EditorStack.js';
import '../../QueryEditor/QueryHeader.js';
import 'react-virtualized-auto-sizer';
import 'sql-formatter';
import '../../SQLEditor/standardSql/language.js';
import 'uuid';
import '../../SQLEditor/utils/debugger.js';
import '../../SQLEditor/standardSql/macros.js';
import '../../QueryEditor/visual-query-builder/AwesomeQueryBuilder.js';
import '../../QueryEditor/visual-query-builder/WhereRow.js';
import '../../QueryEditor/visual-query-builder/EditorField.js';

function QueryOptionGroup({ title, children, collapsedInfo, queryStats }) {
  const [isOpen, toggleOpen] = useToggle(false);
  const styles = useStyles2(getStyles);
  return /* @__PURE__ */ React.createElement("div", { className: styles.wrapper }, /* @__PURE__ */ React.createElement(
    Collapse,
    {
      className: styles.collapse,
      collapsible: true,
      isOpen,
      onToggle: toggleOpen,
      label: /* @__PURE__ */ React.createElement(EditorStack, { gap: 0 }, /* @__PURE__ */ React.createElement("h6", { className: styles.title }, title), !isOpen && /* @__PURE__ */ React.createElement("div", { className: styles.description }, collapsedInfo.map((x, i) => /* @__PURE__ */ React.createElement("span", { key: i }, x))))
    },
    /* @__PURE__ */ React.createElement("div", { className: styles.body }, children)
  ), /* @__PURE__ */ React.createElement("div", { className: styles.queryStatsWrap }, queryStats && config.featureToggles.lokiQuerySplitting && /* @__PURE__ */ React.createElement(Tooltip, { content: "Note: the query will be split into multiple parts and executed in sequence. Query limits will only apply each individual part." }, /* @__PURE__ */ React.createElement(Icon, { tabIndex: 0, name: "info-circle", className: styles.tooltip, size: "sm" })), queryStats && /* @__PURE__ */ React.createElement("p", { className: styles.stats }, generateQueryStats(queryStats))));
}
const getStyles = (theme) => {
  return {
    collapse: css({
      backgroundColor: "unset",
      border: "unset",
      marginBottom: 0,
      ["> button"]: {
        padding: theme.spacing(0, 1)
      }
    }),
    wrapper: css({
      width: "100%",
      display: "flex",
      justifyContent: "space-between",
      alignItems: "baseline"
    }),
    title: css({
      flexGrow: 1,
      overflow: "hidden",
      fontSize: theme.typography.bodySmall.fontSize,
      fontWeight: theme.typography.fontWeightMedium,
      margin: 0
    }),
    description: css({
      color: theme.colors.text.secondary,
      fontSize: theme.typography.bodySmall.fontSize,
      fontWeight: theme.typography.bodySmall.fontWeight,
      paddingLeft: theme.spacing(2),
      gap: theme.spacing(2),
      display: "flex"
    }),
    body: css({
      display: "flex",
      gap: theme.spacing(2),
      flexWrap: "wrap"
    }),
    queryStatsWrap: css({
      display: "flex",
      alignItems: "center",
      alignSelf: "flex-start",
      label: "query-stats-wrap",
      padding: theme.spacing(1)
    }),
    stats: css({
      margin: "0px",
      color: theme.colors.text.secondary,
      fontSize: theme.typography.bodySmall.fontSize
    }),
    tooltip: css({
      marginRight: theme.spacing(0.25)
    })
  };
};
const generateQueryStats = (queryStats) => {
  if (queryStats.message) {
    return queryStats.message;
  }
  return `This query will process approximately ${convertUnits(queryStats)}.`;
};
const convertUnits = (queryStats) => {
  const { text, suffix } = getValueFormat("bytes")(queryStats.bytes, 1);
  return text + suffix;
};

export { QueryOptionGroup };
//# sourceMappingURL=QueryOptionGroup.js.map
