import React, { useState } from 'react';
import { css, cx } from '@emotion/css';
import '@grafana/data';
import { useStyles2, Select, SelectContainer as SelectContainer$1 } from '@grafana/ui';

function InlineSelect({ label: labelProp, ...props }) {
  const [id] = useState(() => Math.random().toString(16).slice(2));
  const styles = useStyles2(getSelectStyles);
  const components = {
    SelectContainer,
    ValueContainer,
    SingleValue: ValueContainer
  };
  return /* @__PURE__ */ React.createElement("div", { className: styles.root }, labelProp && /* @__PURE__ */ React.createElement("label", { className: styles.label, htmlFor: id }, labelProp, ":", "\xA0"), /* @__PURE__ */ React.createElement(Select, { inputId: id, ...props, components }));
}
const SelectContainer = (props) => {
  const { children } = props;
  const styles = useStyles2(getSelectStyles);
  return /* @__PURE__ */ React.createElement(SelectContainer$1, { ...props, className: cx(props.className, styles.container) }, children);
};
const ValueContainer = (props) => {
  const { className, children } = props;
  const styles = useStyles2(getSelectStyles);
  return /* @__PURE__ */ React.createElement("div", { className: cx(className, styles.valueContainer) }, children);
};
const getSelectStyles = (theme) => ({
  root: css({
    display: "flex",
    fontSize: 12,
    alignItems: "center"
  }),
  label: css({
    color: theme.colors.text.secondary,
    whiteSpace: "nowrap"
  }),
  container: css({
    background: "none",
    borderColor: "transparent"
  }),
  valueContainer: css({
    display: "flex",
    alignItems: "center",
    flex: "initial",
    color: theme.colors.text.secondary,
    fontSize: 12
  })
});

export { InlineSelect };
//# sourceMappingURL=InlineSelect.js.map
